#!/bin/sh

if [ -n "${1}" ]; then
	cat ~/.ssh/id_rsa.pub | ssh ${1} "umask 077; test -d ~/.ssh || mkdir ~/.ssh ; cat >> ~/.ssh/authorized_keys" \
else
	echo "Appends ~/.ssh/id_rsa.pub to remotehost's ~/.ssh/authorized_keys"
	echo "Usage:  $0 user@remotehost"
	exit 1
fi
